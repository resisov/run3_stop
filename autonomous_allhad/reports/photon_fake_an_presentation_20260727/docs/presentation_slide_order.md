# Recommended five-slide presentation sequence

## Slide 1 — Method

Use `../figures/main/fake_photon_background_estimation_schematic.pdf`.

Message: A is validation only; C/D measure the fake factor; B predicts the
fake contribution in A after prompt/electron subtraction.

## Slide 2 — Measurement

Use the EB and EE panels from:

- `../figures/main/02_transfer_factors_EB.pdf`;
- `../figures/main/02_transfer_factors_EE.pdf`.

Message: all eight factors are measured directly. EB decreases smoothly with
photon \(p_T\); the highest EE bin is statistically limited.

Put the transfer-factor input plots in backup.

## Slide 3 — Closure and uncertainty

Use:

- `../figures/main/GCR__ut__qcd_closure.pdf`;
- `../figures/main/GCR__ut__systematics.pdf`.

Message: closure is 0.6806; the central estimate is not corrected; 45.18%
nonclosure is assigned as an uncertainty.

## Slide 4 — Why the whole QCD process is not replaced

Use `../figures/main/GCR__ut__qcd_replacement_comparison.pdf`.

Message: the nominal QCD target is 92.62% prompt and only 7.38% truth-fake.
Replacing all QCD worsens Data/MC from 1.4156 to 2.1372. Replacing only the
truth-fake component improves it to 1.3458.

## Slide 5 — Final prefit diagnostic and decision

Use `../figures/main/gcr-ut-prefit-fake-only.pdf`.

Message: the prefit rate and shape improve, but the fake estimate explains
only about 12.5% of the original Data–MC gap. The result remains conditional
pending the QCD/\(\gamma+\)jets overlap review; the nominal intermediate is
unchanged.

## Backup order

1. EB/EE transfer-factor inputs;
2. \(U_T\) application subtraction;
3. \(U_T\) target validation;
4. \(H_T\), \(N_j\), and \(N_b\) validation families;
5. \(N_{\mathrm{top}}=0\) and \(N_{\mathrm{top}}\geq1\) recoil;
6. remaining jet, fat-jet, top-tag, and W-tag plots;
7. normalization and campaign-completeness tables.

